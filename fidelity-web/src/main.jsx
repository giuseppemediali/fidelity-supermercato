import React from "react";
import ReactDOM from "react-dom/client";
import FidelityApp from "./FidelityApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FidelityApp />
  </React.StrictMode>
);
